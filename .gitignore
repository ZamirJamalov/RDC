# Environment files (contain secrets — never commit)
.env
.env.local
.env.*.local
# PR #298: laptop-side secrets for run-remote.sh (never commit)
source/deploy/rdc.env

# Go binaries
*.exe
*.exe~
*.dll
*.so
*.dylib
rdc-source
rdc

# Go test / coverage
*.test
*.out
coverage.txt
coverage.html

# IDE / editor
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Build artifacts
/bin/
/dist/

# PR #306: bütün .env faylları — parol saxlaya bilər (deploy/env.example tutulmur)
*.env
