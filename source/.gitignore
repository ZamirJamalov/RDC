# PR #290: app.log lokal işləmə zamanı yaranır (tee) — koda commit olunmur.
# Promtail source/ qovluğunu mount edib oxuyur (docker-compose.yml).
/app.log

# PR #330: prod bundle generator artifacts (generasiya olunur, commit olunmur)
/deploy/setup-prod.sh
/dist/
/rdc
